<Window x:Class="WPF_Story_05_RoutingEvent.Custom_RouteEvent"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Story_05_RoutingEvent"
        local:TimeButton.ReportTime="ReportTimeHandler"
        mc:Ignorable="d"
        Title="Custom_RouteEvent" Height="450" Width="800"
        x:Name="BaseWindow">
    <Grid x:Name="BaseGrid" local:TimeButton.ReportTime="ReportTimeHandler">
        <StackPanel Name="StackPanel" Margin="20" local:TimeButton.ReportTime="ReportTimeHandler">
            <DockPanel x:Name="DockerPanel" Margin="20" local:TimeButton.ReportTime="ReportTimeHandler">
                <Canvas Name="Canvas" Margin="20" local:TimeButton.ReportTime="ReportTimeHandler">
                    <Grid Name="InsideGrid" Margin="20" local:TimeButton.ReportTime="ReportTimeHandler">
                        <local:TimeButton x:Name="TimeButton"
                                   Width="80" Height="40"
                                   Content="Report"  
                                   local:TimeButton.ReportTime="ReportTimeHandler"/>
                        
                        <ListBox x:Name="ListBox" Margin="0,65,-130,-155"/>
                    </Grid>
                </Canvas>
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
