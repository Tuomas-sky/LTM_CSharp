<Window x:Class="WPF_Story_06_Commands.two_CommandPrameter"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Story_06_Commands"
        mc:Ignorable="d"
        Title="two_CommandPrameter" Height="450" Width="800">
    <!--需求，两个按钮都实现添加条目的功能，其中一个是添加Student信息条目，另一个是添加Teacher信息条目。当姓名和年龄都输入时命令才可用。-->
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Name:" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox x:Name="nameTextBox" Grid.Row="0" Margin="60,0,0,0" />
        <TextBlock Text="Age:" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center"/>
        <TextBox x:Name="ageTextBox" Grid.Row="2" Margin="60,0,0,0"/>
        <Button Content="New Student" Grid.Row="4" Command="New" CommandParameter="Student"/>
        <Button Content="New Teacher" Grid.Row="6" Command="New" CommandParameter="Teacher"/>
        <ListBox x:Name="listBoxNewItem" Grid.Row="8"/>
        <!--对于命令，在任何一个控件上，都可以对其Command属性或CommandBinding使用绑定声明，绑定到其他数据源上。
        <Button Grid.Row="10" Command="{Binding btnSaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}">保存</Button>-->

    </Grid>
    <!--为窗体添加CommandBinding-->
    <Window.CommandBindings>
        <CommandBinding Command="New" CanExecute="New_CanExecute" Executed="New_Executed"/>
    </Window.CommandBindings>
    
</Window>
